# =========================================================================
#   THE BIG BOSS IDEA - UNIFIED ENVIRONMENT CONFIGURATION TEMPLATE
#   Copy this file to .env in the root and fill in your secrets.
# =========================================================================

# Connection string for Neon PostgreSQL database (with SSL)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Port for the Express backend server (default: 5000)
BACKEND_PORT=5000

# Next.js frontend envs (Publicly available to client)
NEXT_PUBLIC_BACKEND_URL="http://localhost:5000"

# Stuck-on Alert Threshold in Milliseconds (e.g., 7200000 = 2 hours, 120000 = 2 minutes for demo)
STUCK_ON_THRESHOLD_MS=7200000

# Gemini AI API Configuration
# Default provider is 'gemini'. Can be swapped or configured.
AI_API_PROVIDER="gemini"
AI_API_KEY="your-gemini-api-key-here"

# Discord Bot Token and Webhook URL
DISCORD_BOT_TOKEN="your-discord-bot-token-here"
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/your-webhook-url-here"
DISCORD_ALLOWED_CHANNEL_ID="your-channel-id-here"
DISCORD_CLIENT_ID="your-discord-client-id-here"
DISCORD_GUILD_ID="your-discord-guild-id-here"

# CORS whitelist (comma-separated). Use * for all origins.
ALLOWED_ORIGINS="*"

# Voice channel ID for real-time load display (e.g., ⚡ Load: 150W)
VOICE_CHANNEL_ID="your-voice-channel-id-here"

# Backend config-override file path (optional)
THRESHOLD_OVERRIDE_FILE="threshold-override.json"

# Cron expressions (optional overrides; defaults shown)
REPORT_CRON_DAILY="0 18 * * *"
REPORT_CRON_WEEKLY="0 9 * * 1"

# Timezone for node-cron (optional, default UTC)
TZ="UTC"

# Logging level: trace | debug | info | warn | error
LOG_LEVEL="info"
